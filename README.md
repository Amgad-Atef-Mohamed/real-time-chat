# The Instactions For Running Chat:
> Installation steps:
```
* npm install
* npm install sequelize-cli -g
* npm install -g nodemon
```

> How To Run Application:
```
* sequelize db:migrate --config config/dbconfig.json
* sequelize db:seed:all --config config/dbconfig.json
* nodemon server.js
```